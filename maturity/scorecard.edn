{:maturity/as-of "2026-07-10"
 :maturity/level :r6-consumer-ready
 :maturity/criteria
 [{:criterion :separate-source-datasets :status :met :evidence ["etzhayyim/jp.go.meti.enecho" "etzhayyim/org.worldbank.api" "etzhayyim/org.ourworldindata" "etzhayyim/org.un.unstats"]}
  {:criterion :raw-integrity :status :partial :evidence "通常Gitの原典はCIでSHA-256検証。B2 git-annex原典はCIでポインタ検証、実体fsckはB2認証を持つ保全環境で実行"}
  {:criterion :semantic-schema :status :met :evidence "schema/energy.edn"}
  {:criterion :provenance-per-observation :status :met :evidence [:energy.observation/source :energy.observation/release :energy/year]}
  {:criterion :datascript-query-verification :status :met :evidence "queries/examples.edn を Datascript db-with で実行"}
  {:criterion :kotoba-eavt-export :status :met :evidence "data/global-energy.kotoba.edn"}
  {:criterion :consumer-contracts :status :met :evidence "connections/actors.edn"}
  {:criterion :locked-source-revisions :status :met :evidence "sources.lock.edn + contract.yml の immutable revision checkout"}
  {:criterion :ci-contract-validation :status :met :evidence "GitHub Actions: source hash verification → deterministic build → generated diff gate → Datascript contract test"}
  {:criterion :automated-refresh :status :met :evidence ".github/workflows/upstream-drift.yml。週次のread-only drift reportで、raw・lock・公開物を自動更新しない"}
  {:criterion :read-only-consumer-adapter :status :met :evidence "adapters/read_only.clj + connections/actors.edn"}
  {:criterion :official-m49-iso3-crosswalk :status :met :evidence "org.un.unstats/raw/m49/overview-en.html → derived/m49-iso3.edn。UN SDG entityへ :country/iso3 を付与"}]}
